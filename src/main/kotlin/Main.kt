package dev.janku.learn.kotlincodewars

/**
 * Run/Executre shortcuts:
 *  Control (+ Shift) + R   run the main function
 *  Control (+ Shift) + D   debug the main function
 *
 * IDE shortcuts:
 *  Option + Enter          actions menu
 *  Cmd + N                 generating code
 *
 * Navigation:
 *  Cmd + [                 navigate back
 *  Cmd + ]                 navigate forward
 *  Cmd + Shift + [/]       navigate between tabs
 *  Cmd (+ Shift) + E       recent (edited) files
 *
 *  Panels:
 *   Cmd + 1                project
*/
fun main() {
    val name = "Kotlin"
    println("Hello, $name!")

    for (i in 1..5) {
        println("i = $i")
    }
}